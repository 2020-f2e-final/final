<div class="mx-auto w-11/12">
    <br/>
    <br/>
    <div x-data="{ tab: 'level' }" >
        <div class="grid grid-cols-10 gap-1">
            <div class="col-start-1 text-6xl relative left-2 font-bold cursor-pointer text-main-levelTitle-light" @click="tab = 'level'">等級</div>
            <div class="col-start-6 text-6xl font-bold cursor-pointer text-main-levelTitle-dark" @click="tab = 'licence'">駕照</div>
        </div>

        <div x-show="tab === 'level'">
            <%- include('level') %>
        </div>
        <div x-show="tab === 'licence'"></div>
    </div>
</div>
